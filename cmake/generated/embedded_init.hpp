// Auto-generated by CMake. DO NOT EDIT.
#pragma once

namespace Embedded {
inline constexpr const char* INIT_LUA = R"LUA_EOF(
-- ~/.config/interactive-wallpaper/init.lua
-- Это ГЛАВНЫЙ конфигурационный файл. Сюда можно писать любую Lua логику.
_G.ctl = require("ctl")
-- ==============================================================================
-- 1. НАСТРОЙКИ ЯДРА (Базовые параметры)
-- ==============================================================================
core = core or {}
core.default_effect = "Icosahedron Sphere" -- Эффект по умолчанию для всех мониторов
core.interactive = true

-- ==============================================================================
-- 2. МУЛЬТИМОНИТОРНАЯ КОНФИГУРАЦИЯ (Per-Output Routing)
-- Раскомментируйте и настройте под имена ваших экранов (например: DP-1, HDMI-A-1)
-- ==============================================================================
 core.outputs = {
     ["eDP-1"] = {
         effect = "Icosahedron Sphere Old",
         preset = "cyberpunk",
         
         settings = {
             wireframe_mode = true,
             subdivisions = 5,
             sphere_scale = 1.0,
             bloom_intensity = 0.0
             
         }
     },
     ["WL-2"] = {
         effect = "Icosahedron Sphere",
         preset = "minimal_white",

         settings = { 
            speed = 2.0,
            subdivisions = 6,
            background_color = { 0.85, 0.90, 0.95 },   -- Строгий глубокий черный
    wireframe_color = { 0.02, 0.02, 0.02 },
    sphere_scale = 5.0
          }
         
     },

     ["WL-3"] = {
         effect = "3D Raymarched Text",
         settings = { 
            speed = 2.0,
            subdivisions = 6,
          }
         
     }
 }

-- ==============================================================================
-- 3. НАСТРОЙКИ ПРОВАЙДЕРОВ ДАННЫХ (Smart Providers)
-- ==============================================================================
core.providers = {
    ["Evdev Pointer Provider"] = {
        enabled = true,
        mouse_sensitivity = 1.0,
        touchpad_sensitivity = 3.0,
        invert_x = false,
        invert_y = false
    },
    ["Cava Audio Provider"] = {
        enabled = true,
        smoothing = 0.95,
        volume_multiplier = 1.0,
        bass_multiplier = 1.2,
        mid_multiplier = 1.0,
        treble_multiplier = 10.2
    }
}


-- ==============================================================================
-- 4. ГЛОБАЛЬНЫЙ ДИСПЕТЧЕР АНИМАЦИЙ (Animation Router)
-- ==============================================================================
core.on_frame(function(dt, output_name)
    -- 1. Получаем конфиг текущего монитора из нашей таблицы
    local out_conf = core.outputs[output_name]
    if not out_conf then return end
    
    -- 2. Проверяем, загружена ли для этого экрана индивидуальная анимация
    -- (Когда C++ вызывает apply_preset, он копирует on_frame в таблицу settings)
    if out_conf.settings and type(out_conf.settings.on_frame) == "function" then
        -- Выполняем код анимации из файла пресета:
        out_conf.settings.on_frame(dt, output_name)
    end

    
end)

function toggle_param(output, param)
    local s = core.outputs[output].settings
    s[param] = not s[param]
    core.set_effect_param(output, param, s[param])
end)LUA_EOF";
} // namespace Embedded
